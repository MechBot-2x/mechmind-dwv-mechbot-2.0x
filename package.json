{
  "dependencies": {
    "mime-types": "2.1.35",
    "node-fetch-retry": "1.1.1"
  }
{
"scripts": {
  "test": "echo \"Error: no test specified\" && exit 1"
  // o tu comando real de tests si lo tienes
}
